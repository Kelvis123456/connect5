// ColumnButton is no longer used. Column click/hover handling is done
// directly inside GameUI via UI Toolkit VisualElement callbacks.
// This stub is kept only to avoid breaking any saved scene references.
using UnityEngine;

public class ColumnButton : MonoBehaviour { }
